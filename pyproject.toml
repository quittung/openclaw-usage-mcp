[project]
name = "openclaw-usage-mcp"
version = "0.1.0"
description = "MCP server exposing read-only usage/cost queries for an OpenClaw gateway"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0",
]
